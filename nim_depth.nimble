# Package

version       = "0.1.0"
author        = "Brent Pedersen"
description   = "fast depth"
license       = "MIT"

# Dependencies

requires "nim >= 0.17.0", "hts >= 0.1.0", "docopt", "binaryheap"

bin = @["nimdepth"]
